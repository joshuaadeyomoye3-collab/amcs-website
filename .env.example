# Google Apps Script web-app URL used to store form submissions in a Google
# Sheet AND email the cooperative when someone joins or sends a message.
#
# Setup steps are in the README ("Form submissions → Google Sheets + email").
# After deploying the Apps Script web app, paste its URL here (and in your
# Vercel project's Environment Variables).
SHEETS_WEBHOOK_URL=
